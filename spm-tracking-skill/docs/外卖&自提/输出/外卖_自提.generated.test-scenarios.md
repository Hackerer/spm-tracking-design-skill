# 埋点测试建议

- 需求名称：未命名需求

## 结算页页面浏览验证

- 页面：结算页
- 目标SPM：takeaway_pickup.checkout
- 测试类型：pv_check
- 预期结果：页面浏览上报成功，页面SPM与page_id符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 页面进入 | 进入结算页 | 页面浏览上报 | 确认页面浏览是否正常上报，page_id是否正确。 |
## 商品确认区区块曝光验证

- 页面：结算页
- 目标SPM：takeaway_pickup.checkout.product_confirm
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入结算页并确认商品确认区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 商品确认卡片参数验证

- 页面：结算页
- 目标SPM：takeaway_pickup.checkout.product_confirm.confirm_card
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 店铺ID参数校验 | 触发商品确认卡片对应事件 | shop_id | 店铺ID需符合类型 string。 |
| 商品ID参数校验 | 触发商品确认卡片对应事件 | sku_id | 商品ID需符合类型 string，且为必填。 |
| 配送方式参数校验 | 触发商品确认卡片对应事件 | delivery_type | 配送方式需符合类型 string。 |
| 用户PIN参数校验 | 触发商品确认卡片对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发商品确认卡片对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发商品确认卡片对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发商品确认卡片对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发商品确认卡片对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发商品确认卡片对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发商品确认卡片对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 结算信息区区块曝光验证

- 页面：结算页
- 目标SPM：takeaway_pickup.checkout.settlement_panel
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入结算页并确认结算信息区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 地址卡片参数验证

- 页面：结算页
- 目标SPM：takeaway_pickup.checkout.settlement_panel.address_card
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发地址卡片对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发地址卡片对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发地址卡片对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发地址卡片对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发地址卡片对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发地址卡片对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发地址卡片对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
| 配送方式参数校验 | 触发地址卡片对应事件 | delivery_type | 配送方式需符合类型 string。 |
## 支付方式入口坑位点击验证

- 页面：结算页
- 目标SPM：takeaway_pickup.checkout.payment_section.payment_method_entry
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击支付方式入口 | 点击事件上报 | 无biz_type要求。 |
## 支付方式入口参数验证

- 页面：结算页
- 目标SPM：takeaway_pickup.checkout.payment_section.payment_method_entry
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 支付方式参数校验 | 触发支付方式入口对应事件 | payment_method | 支付方式需符合类型 string。 |
| 用户PIN参数校验 | 触发支付方式入口对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发支付方式入口对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发支付方式入口对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发支付方式入口对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发支付方式入口对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发支付方式入口对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发支付方式入口对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
| 配送方式参数校验 | 触发支付方式入口对应事件 | delivery_type | 配送方式需符合类型 string。 |
## 去支付按钮坑位点击验证

- 页面：结算页
- 目标SPM：takeaway_pickup.checkout.fixed_bottom.pay_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击去支付按钮 | 点击事件上报 | 无biz_type要求。 |
## 去支付按钮参数验证

- 页面：结算页
- 目标SPM：takeaway_pickup.checkout.fixed_bottom.pay_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 配送方式参数校验 | 触发去支付按钮对应事件 | delivery_type | 配送方式需符合类型 string。 |
| 价格参数校验 | 触发去支付按钮对应事件 | price | 价格需符合类型 number。 |
| 用户PIN参数校验 | 触发去支付按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发去支付按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发去支付按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发去支付按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发去支付按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发去支付按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发去支付按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 支付中页页面浏览验证

- 页面：支付中页
- 目标SPM：takeaway_pickup.paying
- 测试类型：pv_check
- 预期结果：页面浏览上报成功，页面SPM与page_id符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 页面进入 | 进入支付中页 | 页面浏览上报 | 确认页面浏览是否正常上报，page_id是否正确。 |
## 支付状态区区块曝光验证

- 页面：支付中页
- 目标SPM：takeaway_pickup.paying.payment_status
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入支付中页并确认支付状态区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 支付中状态参数验证

- 页面：支付中页
- 目标SPM：takeaway_pickup.paying.payment_status.paying_status
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发支付中状态对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发支付中状态对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发支付中状态对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发支付中状态对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发支付中状态对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发支付中状态对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发支付中状态对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 支付成功页页面浏览验证

- 页面：支付成功页
- 目标SPM：takeaway_pickup.pay_success
- 测试类型：pv_check
- 预期结果：页面浏览上报成功，页面SPM与page_id符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 页面进入 | 进入支付成功页 | 页面浏览上报 | 确认页面浏览是否正常上报，page_id是否正确。 |
## 支付状态区区块曝光验证

- 页面：支付成功页
- 目标SPM：takeaway_pickup.pay_success.payment_status
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入支付成功页并确认支付状态区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 支付成功状态参数验证

- 页面：支付成功页
- 目标SPM：takeaway_pickup.pay_success.payment_status.pay_success_status
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发支付成功状态对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发支付成功状态对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发支付成功状态对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发支付成功状态对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发支付成功状态对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发支付成功状态对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发支付成功状态对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 查看订单按钮坑位点击验证

- 页面：支付成功页
- 目标SPM：takeaway_pickup.pay_success.result_actions.view_order_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击查看订单按钮 | 点击事件上报 | 无biz_type要求。 |
## 查看订单按钮参数验证

- 页面：支付成功页
- 目标SPM：takeaway_pickup.pay_success.result_actions.view_order_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发查看订单按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发查看订单按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发查看订单按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发查看订单按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发查看订单按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发查看订单按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发查看订单按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 继续推荐按钮坑位点击验证

- 页面：支付成功页
- 目标SPM：takeaway_pickup.pay_success.result_actions.recommend_food_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击继续推荐按钮 | 点击事件上报 | 无biz_type要求。 |
## 继续推荐按钮参数验证

- 页面：支付成功页
- 目标SPM：takeaway_pickup.pay_success.result_actions.recommend_food_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 推荐Sug词参数校验 | 触发继续推荐按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
| 用户PIN参数校验 | 触发继续推荐按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发继续推荐按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发继续推荐按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发继续推荐按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发继续推荐按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发继续推荐按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
## 商品确认页页面浏览验证

- 页面：商品确认页
- 目标SPM：takeaway_pickup.item_confirm
- 测试类型：pv_check
- 预期结果：页面浏览上报成功，页面SPM与page_id符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 页面进入 | 进入商品确认页 | 页面浏览上报 | 确认页面浏览是否正常上报，page_id是否正确。 |
## 商品确认区区块曝光验证

- 页面：商品确认页
- 目标SPM：takeaway_pickup.item_confirm.product_confirm
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入商品确认页并确认商品确认区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 商品确认卡片参数验证

- 页面：商品确认页
- 目标SPM：takeaway_pickup.item_confirm.product_confirm.confirm_card
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 店铺ID参数校验 | 触发商品确认卡片对应事件 | shop_id | 店铺ID需符合类型 string。 |
| 商品ID参数校验 | 触发商品确认卡片对应事件 | sku_id | 商品ID需符合类型 string，且为必填。 |
| 配送方式参数校验 | 触发商品确认卡片对应事件 | delivery_type | 配送方式需符合类型 string。 |
| 用户PIN参数校验 | 触发商品确认卡片对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发商品确认卡片对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发商品确认卡片对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发商品确认卡片对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发商品确认卡片对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发商品确认卡片对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发商品确认卡片对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 切换热饮按钮坑位点击验证

- 页面：商品确认页
- 目标SPM：takeaway_pickup.item_confirm.confirm_actions.switch_hot_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击切换热饮按钮 | 点击事件上报 | 无biz_type要求。 |
## 切换热饮按钮参数验证

- 页面：商品确认页
- 目标SPM：takeaway_pickup.item_confirm.confirm_actions.switch_hot_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 规格信息参数校验 | 触发切换热饮按钮对应事件 | spec | 规格信息需符合类型 string。 |
| 用户PIN参数校验 | 触发切换热饮按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发切换热饮按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发切换热饮按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发切换热饮按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发切换热饮按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发切换热饮按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发切换热饮按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
| 配送方式参数校验 | 触发切换热饮按钮对应事件 | delivery_type | 配送方式需符合类型 string。 |
## 切换自提按钮坑位点击验证

- 页面：商品确认页
- 目标SPM：takeaway_pickup.item_confirm.confirm_actions.switch_self_pickup_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击切换自提按钮 | 点击事件上报 | 无biz_type要求。 |
## 切换自提按钮参数验证

- 页面：商品确认页
- 目标SPM：takeaway_pickup.item_confirm.confirm_actions.switch_self_pickup_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 配送方式参数校验 | 触发切换自提按钮对应事件 | delivery_type | 配送方式需符合类型 string。 |
| 用户PIN参数校验 | 触发切换自提按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发切换自提按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发切换自提按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发切换自提按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发切换自提按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发切换自提按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发切换自提按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 去支付按钮坑位点击验证

- 页面：商品确认页
- 目标SPM：takeaway_pickup.item_confirm.fixed_bottom.pay_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击去支付按钮 | 点击事件上报 | 无biz_type要求。 |
## 去支付按钮参数验证

- 页面：商品确认页
- 目标SPM：takeaway_pickup.item_confirm.fixed_bottom.pay_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 配送方式参数校验 | 触发去支付按钮对应事件 | delivery_type | 配送方式需符合类型 string。 |
| 价格参数校验 | 触发去支付按钮对应事件 | price | 价格需符合类型 number。 |
| 用户PIN参数校验 | 触发去支付按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发去支付按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发去支付按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发去支付按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发去支付按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发去支付按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发去支付按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 订单详情页页面浏览验证

- 页面：订单详情页
- 目标SPM：takeaway_pickup.order_detail
- 测试类型：pv_check
- 预期结果：页面浏览上报成功，页面SPM与page_id符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 页面进入 | 进入订单详情页 | 页面浏览上报 | 确认页面浏览是否正常上报，page_id是否正确。 |
## 结算信息区区块曝光验证

- 页面：订单详情页
- 目标SPM：takeaway_pickup.order_detail.settlement_panel
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入订单详情页并确认结算信息区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 订单卡片坑位点击验证

- 页面：订单详情页
- 目标SPM：takeaway_pickup.order_detail.settlement_panel.order_card
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击订单卡片 | 点击事件上报 | 无biz_type要求。 |
## 订单卡片参数验证

- 页面：订单详情页
- 目标SPM：takeaway_pickup.order_detail.settlement_panel.order_card
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发订单卡片对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发订单卡片对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发订单卡片对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发订单卡片对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发订单卡片对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发订单卡片对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发订单卡片对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
| 配送方式参数校验 | 触发订单卡片对应事件 | delivery_type | 配送方式需符合类型 string。 |
## 继续推荐按钮坑位点击验证

- 页面：订单详情页
- 目标SPM：takeaway_pickup.order_detail.detail_actions.recommend_food_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击继续推荐按钮 | 点击事件上报 | 无biz_type要求。 |
## 继续推荐按钮参数验证

- 页面：订单详情页
- 目标SPM：takeaway_pickup.order_detail.detail_actions.recommend_food_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 推荐Sug词参数校验 | 触发继续推荐按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
| 用户PIN参数校验 | 触发继续推荐按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发继续推荐按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发继续推荐按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发继续推荐按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发继续推荐按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发继续推荐按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 配送方式参数校验 | 触发继续推荐按钮对应事件 | delivery_type | 配送方式需符合类型 string。 |
## 多品确认页页面浏览验证

- 页面：多品确认页
- 目标SPM：takeaway_pickup.multi_item_confirm
- 测试类型：pv_check
- 预期结果：页面浏览上报成功，页面SPM与page_id符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 页面进入 | 进入多品确认页 | 页面浏览上报 | 确认页面浏览是否正常上报，page_id是否正确。 |
## 商品确认区区块曝光验证

- 页面：多品确认页
- 目标SPM：takeaway_pickup.multi_item_confirm.product_confirm
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入多品确认页并确认商品确认区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 商品确认卡片坑位曝光验证

- 页面：多品确认页
- 目标SPM：takeaway_pickup.multi_item_confirm.product_confirm.confirm_item_card
- 测试类型：ordered_position_check
- 预期结果：坑位曝光上报成功，SPM位置定义与方案一致。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 坑位曝光 | 确认商品确认卡片展示并进入可视区域 | 坑位曝光上报 | 顺序坑位需同时确认坑位顺序是否符合预期。顺序坑位需要区分具体位置或序号，坑位级曝光具备独立分析价值。 |
## 商品确认卡片参数验证

- 页面：多品确认页
- 目标SPM：takeaway_pickup.multi_item_confirm.product_confirm.confirm_item_card
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 店铺ID参数校验 | 触发商品确认卡片对应事件 | shop_id | 店铺ID需符合类型 string。 |
| 商品ID参数校验 | 触发商品确认卡片对应事件 | sku_id | 商品ID需符合类型 string，且为必填。 |
| 配送方式参数校验 | 触发商品确认卡片对应事件 | delivery_type | 配送方式需符合类型 string。 |
| 用户PIN参数校验 | 触发商品确认卡片对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发商品确认卡片对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发商品确认卡片对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发商品确认卡片对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发商品确认卡片对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发商品确认卡片对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发商品确认卡片对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 切换热饮按钮坑位点击验证

- 页面：多品确认页
- 目标SPM：takeaway_pickup.multi_item_confirm.confirm_actions.switch_hot_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击切换热饮按钮 | 点击事件上报 | 无biz_type要求。 |
## 切换热饮按钮参数验证

- 页面：多品确认页
- 目标SPM：takeaway_pickup.multi_item_confirm.confirm_actions.switch_hot_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 规格信息参数校验 | 触发切换热饮按钮对应事件 | spec | 规格信息需符合类型 string。 |
| 用户PIN参数校验 | 触发切换热饮按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发切换热饮按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发切换热饮按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发切换热饮按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发切换热饮按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发切换热饮按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发切换热饮按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
| 配送方式参数校验 | 触发切换热饮按钮对应事件 | delivery_type | 配送方式需符合类型 string。 |
## 切换自提按钮坑位点击验证

- 页面：多品确认页
- 目标SPM：takeaway_pickup.multi_item_confirm.confirm_actions.switch_self_pickup_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击切换自提按钮 | 点击事件上报 | 无biz_type要求。 |
## 切换自提按钮参数验证

- 页面：多品确认页
- 目标SPM：takeaway_pickup.multi_item_confirm.confirm_actions.switch_self_pickup_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 配送方式参数校验 | 触发切换自提按钮对应事件 | delivery_type | 配送方式需符合类型 string。 |
| 用户PIN参数校验 | 触发切换自提按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发切换自提按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发切换自提按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发切换自提按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发切换自提按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发切换自提按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发切换自提按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 去支付按钮坑位点击验证

- 页面：多品确认页
- 目标SPM：takeaway_pickup.multi_item_confirm.fixed_bottom.pay_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击去支付按钮 | 点击事件上报 | 无biz_type要求。 |
## 去支付按钮参数验证

- 页面：多品确认页
- 目标SPM：takeaway_pickup.multi_item_confirm.fixed_bottom.pay_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 配送方式参数校验 | 触发去支付按钮对应事件 | delivery_type | 配送方式需符合类型 string。 |
| 价格参数校验 | 触发去支付按钮对应事件 | price | 价格需符合类型 number。 |
| 用户PIN参数校验 | 触发去支付按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发去支付按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发去支付按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发去支付按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发去支付按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发去支付按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发去支付按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 规格页页面浏览验证

- 页面：规格页
- 目标SPM：takeaway_pickup.spec_select
- 测试类型：pv_check
- 预期结果：页面浏览上报成功，页面SPM与page_id符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 页面进入 | 进入规格页 | 页面浏览上报 | 确认页面浏览是否正常上报，page_id是否正确。 |
## 规格选项坑位曝光验证

- 页面：规格页
- 目标SPM：takeaway_pickup.spec_select.spec_options.spec_option
- 测试类型：ordered_position_check
- 预期结果：坑位曝光上报成功，SPM位置定义与方案一致。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 坑位曝光 | 确认规格选项展示并进入可视区域 | 坑位曝光上报 | 顺序坑位需同时确认坑位顺序是否符合预期。顺序坑位需要区分具体位置或序号，坑位级曝光具备独立分析价值。 |
## 规格选项坑位点击验证

- 页面：规格页
- 目标SPM：takeaway_pickup.spec_select.spec_options.spec_option
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击规格选项 | 点击事件上报 | 无biz_type要求。 |
## 规格选项参数验证

- 页面：规格页
- 目标SPM：takeaway_pickup.spec_select.spec_options.spec_option
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 规格信息参数校验 | 触发规格选项对应事件 | spec | 规格信息需符合类型 string。 |
| 用户PIN参数校验 | 触发规格选项对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发规格选项对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发规格选项对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发规格选项对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发规格选项对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发规格选项对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发规格选项对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 确认规格按钮坑位点击验证

- 页面：规格页
- 目标SPM：takeaway_pickup.spec_select.fixed_bottom.confirm_spec_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击确认规格按钮 | 点击事件上报 | 无biz_type要求。 |
## 确认规格按钮参数验证

- 页面：规格页
- 目标SPM：takeaway_pickup.spec_select.fixed_bottom.confirm_spec_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 规格信息参数校验 | 触发确认规格按钮对应事件 | spec | 规格信息需符合类型 string。 |
| 用户PIN参数校验 | 触发确认规格按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发确认规格按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发确认规格按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发确认规格按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发确认规格按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发确认规格按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发确认规格按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 商品列表页页面浏览验证

- 页面：商品列表页
- 目标SPM：takeaway_pickup.product_list
- 测试类型：pv_check
- 预期结果：页面浏览上报成功，页面SPM与page_id符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 页面进入 | 进入商品列表页 | 页面浏览上报 | 确认页面浏览是否正常上报，page_id是否正确。 |
## 门店头部区区块曝光验证

- 页面：商品列表页
- 目标SPM：takeaway_pickup.product_list.store_header
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入商品列表页并确认门店头部区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 门店头部卡片参数验证

- 页面：商品列表页
- 目标SPM：takeaway_pickup.product_list.store_header.store_summary_card
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发门店头部卡片对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发门店头部卡片对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发门店头部卡片对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发门店头部卡片对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发门店头部卡片对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发门店头部卡片对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发门店头部卡片对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
| 配送方式参数校验 | 触发门店头部卡片对应事件 | delivery_type | 配送方式需符合类型 string。 |
## 商品列表区区块曝光验证

- 页面：商品列表页
- 目标SPM：takeaway_pickup.product_list.product_list
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入商品列表页并确认商品列表区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 商品卡片坑位曝光验证

- 页面：商品列表页
- 目标SPM：takeaway_pickup.product_list.product_list.card
- 测试类型：ordered_position_check
- 预期结果：坑位曝光上报成功，SPM位置定义与方案一致。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 坑位曝光 | 确认商品卡片展示并进入可视区域 | 坑位曝光上报 | 顺序坑位需同时确认坑位顺序是否符合预期。顺序坑位需要区分具体位置或序号，坑位级曝光具备独立分析价值。 |
## 商品卡片坑位点击验证

- 页面：商品列表页
- 目标SPM：takeaway_pickup.product_list.product_list.card
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击商品卡片 | 点击事件上报 | 无biz_type要求。 |
## 商品卡片参数验证

- 页面：商品列表页
- 目标SPM：takeaway_pickup.product_list.product_list.card
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 店铺ID参数校验 | 触发商品卡片对应事件 | shop_id | 店铺ID需符合类型 string。 |
| 商品ID参数校验 | 触发商品卡片对应事件 | sku_id | 商品ID需符合类型 string，且为必填。 |
| 配送方式参数校验 | 触发商品卡片对应事件 | delivery_type | 配送方式需符合类型 string。 |
| 用户PIN参数校验 | 触发商品卡片对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发商品卡片对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发商品卡片对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发商品卡片对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发商品卡片对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发商品卡片对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发商品卡片对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 换一批按钮坑位点击验证

- 页面：商品列表页
- 目标SPM：takeaway_pickup.product_list.list_actions.refresh_batch
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击换一批按钮 | 点击事件上报 | 无biz_type要求。 |
## 换一批按钮参数验证

- 页面：商品列表页
- 目标SPM：takeaway_pickup.product_list.list_actions.refresh_batch
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发换一批按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发换一批按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发换一批按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发换一批按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发换一批按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发换一批按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发换一批按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
| 配送方式参数校验 | 触发换一批按钮对应事件 | delivery_type | 配送方式需符合类型 string。 |
## 快捷下单按钮坑位点击验证

- 页面：商品列表页
- 目标SPM：takeaway_pickup.product_list.list_actions.quick_order_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击快捷下单按钮 | 点击事件上报 | 无biz_type要求。 |
## 快捷下单按钮参数验证

- 页面：商品列表页
- 目标SPM：takeaway_pickup.product_list.list_actions.quick_order_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发快捷下单按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发快捷下单按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发快捷下单按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发快捷下单按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发快捷下单按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发快捷下单按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发快捷下单按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
| 配送方式参数校验 | 触发快捷下单按钮对应事件 | delivery_type | 配送方式需符合类型 string。 |
## 声纹开通弹窗页面浏览验证

- 页面：声纹开通弹窗
- 目标SPM：takeaway_pickup.voiceprint_open
- 测试类型：pv_check
- 预期结果：页面浏览上报成功，页面SPM与page_id符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 页面进入 | 进入声纹开通弹窗 | 页面浏览上报 | 确认页面浏览是否正常上报，page_id是否正确。 |
## 关闭按钮坑位点击验证

- 页面：声纹开通弹窗
- 目标SPM：takeaway_pickup.voiceprint_open.modal_close.close_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击关闭按钮 | 点击事件上报 | 无biz_type要求。 |
## 关闭按钮参数验证

- 页面：声纹开通弹窗
- 目标SPM：takeaway_pickup.voiceprint_open.modal_close.close_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 监测ID参数校验 | 触发关闭按钮对应事件 | monitor_id | 监测ID需符合类型 string。 |
| 用户PIN参数校验 | 触发关闭按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发关闭按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发关闭按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发关闭按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发关闭按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发关闭按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发关闭按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 弹窗正文区区块曝光验证

- 页面：声纹开通弹窗
- 目标SPM：takeaway_pickup.voiceprint_open.modal_body
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入声纹开通弹窗并确认弹窗正文区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 知情同意书链接坑位点击验证

- 页面：声纹开通弹窗
- 目标SPM：takeaway_pickup.voiceprint_open.modal_body.agreement_link
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击知情同意书链接 | 点击事件上报 | 无biz_type要求。 |
## 知情同意书链接参数验证

- 页面：声纹开通弹窗
- 目标SPM：takeaway_pickup.voiceprint_open.modal_body.agreement_link
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 协议类型参数校验 | 触发知情同意书链接对应事件 | agreement_type | 协议类型需符合类型 string。 |
| 用户PIN参数校验 | 触发知情同意书链接对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发知情同意书链接对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发知情同意书链接对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发知情同意书链接对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发知情同意书链接对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发知情同意书链接对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发知情同意书链接对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 操作弹窗区区块曝光验证

- 页面：声纹开通弹窗
- 目标SPM：takeaway_pickup.voiceprint_open.modal_actions
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入声纹开通弹窗并确认操作弹窗区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 暂不按钮坑位点击验证

- 页面：声纹开通弹窗
- 目标SPM：takeaway_pickup.voiceprint_open.modal_actions.skip_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击暂不按钮 | 点击事件上报 | 无biz_type要求。 |
## 暂不按钮参数验证

- 页面：声纹开通弹窗
- 目标SPM：takeaway_pickup.voiceprint_open.modal_actions.skip_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发暂不按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发暂不按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发暂不按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发暂不按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发暂不按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发暂不按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发暂不按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 开通声纹支付按钮坑位点击验证

- 页面：声纹开通弹窗
- 目标SPM：takeaway_pickup.voiceprint_open.modal_actions.enable_voiceprint_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击开通声纹支付按钮 | 点击事件上报 | 无biz_type要求。 |
## 开通声纹支付按钮参数验证

- 页面：声纹开通弹窗
- 目标SPM：takeaway_pickup.voiceprint_open.modal_actions.enable_voiceprint_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发开通声纹支付按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发开通声纹支付按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发开通声纹支付按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发开通声纹支付按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发开通声纹支付按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发开通声纹支付按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发开通声纹支付按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 首页页面浏览验证

- 页面：首页
- 目标SPM：takeaway_pickup.home
- 测试类型：pv_check
- 预期结果：页面浏览上报成功，页面SPM与page_id符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 页面进入 | 进入首页 | 页面浏览上报 | 确认页面浏览是否正常上报，page_id是否正确。 |
## 首页推荐词区区块曝光验证

- 页面：首页
- 目标SPM：takeaway_pickup.home.home_suggestions
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入首页并确认首页推荐词区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 推荐词按钮（周边外卖/nearby_takeaway）坑位曝光验证

- 页面：首页
- 目标SPM：takeaway_pickup.home.home_suggestions.suggestion_btn(nearby_takeaway)
- 测试类型：ordered_position_check
- 预期结果：坑位曝光上报成功，SPM位置定义与方案一致。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 坑位曝光 | 确认推荐词按钮（周边外卖/nearby_takeaway）展示并进入可视区域 | 坑位曝光上报 | 顺序坑位需同时确认坑位顺序是否符合预期。该元素具备独立曝光分析价值，保留坑位级曝光。 |
## 推荐词按钮（周边外卖/nearby_takeaway）坑位点击验证

- 页面：首页
- 目标SPM：takeaway_pickup.home.home_suggestions.suggestion_btn(nearby_takeaway)
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击推荐词按钮（周边外卖/nearby_takeaway） | 点击事件上报 | 需确认biz_type=nearby_takeaway。 |
## 推荐词按钮（周边外卖/nearby_takeaway）参数验证

- 页面：首页
- 目标SPM：takeaway_pickup.home.home_suggestions.suggestion_btn(nearby_takeaway)
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 推荐Sug词参数校验 | 触发推荐词按钮（周边外卖/nearby_takeaway）对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
| 用户PIN参数校验 | 触发推荐词按钮（周边外卖/nearby_takeaway）对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发推荐词按钮（周边外卖/nearby_takeaway）对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发推荐词按钮（周边外卖/nearby_takeaway）对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发推荐词按钮（周边外卖/nearby_takeaway）对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发推荐词按钮（周边外卖/nearby_takeaway）对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发推荐词按钮（周边外卖/nearby_takeaway）对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
## 推荐词按钮（推荐咖啡/recommend_coffee）坑位曝光验证

- 页面：首页
- 目标SPM：takeaway_pickup.home.home_suggestions.suggestion_btn(recommend_coffee)
- 测试类型：ordered_position_check
- 预期结果：坑位曝光上报成功，SPM位置定义与方案一致。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 坑位曝光 | 确认推荐词按钮（推荐咖啡/recommend_coffee）展示并进入可视区域 | 坑位曝光上报 | 顺序坑位需同时确认坑位顺序是否符合预期。该元素具备独立曝光分析价值，保留坑位级曝光。 |
## 推荐词按钮（推荐咖啡/recommend_coffee）坑位点击验证

- 页面：首页
- 目标SPM：takeaway_pickup.home.home_suggestions.suggestion_btn(recommend_coffee)
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击推荐词按钮（推荐咖啡/recommend_coffee） | 点击事件上报 | 需确认biz_type=recommend_coffee。 |
## 推荐词按钮（推荐咖啡/recommend_coffee）参数验证

- 页面：首页
- 目标SPM：takeaway_pickup.home.home_suggestions.suggestion_btn(recommend_coffee)
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 推荐Sug词参数校验 | 触发推荐词按钮（推荐咖啡/recommend_coffee）对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
| 用户PIN参数校验 | 触发推荐词按钮（推荐咖啡/recommend_coffee）对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发推荐词按钮（推荐咖啡/recommend_coffee）对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发推荐词按钮（推荐咖啡/recommend_coffee）对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发推荐词按钮（推荐咖啡/recommend_coffee）对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发推荐词按钮（推荐咖啡/recommend_coffee）对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发推荐词按钮（推荐咖啡/recommend_coffee）对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
## 首页订单卡片区区块曝光验证

- 页面：首页
- 目标SPM：takeaway_pickup.home.home_order_card
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入首页并确认首页订单卡片区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 订单卡片坑位点击验证

- 页面：首页
- 目标SPM：takeaway_pickup.home.home_order_card.order_card
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击订单卡片 | 点击事件上报 | 无biz_type要求。 |
## 订单卡片参数验证

- 页面：首页
- 目标SPM：takeaway_pickup.home.home_order_card.order_card
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发订单卡片对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发订单卡片对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发订单卡片对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发订单卡片对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发订单卡片对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发订单卡片对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发订单卡片对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 地址确认页页面浏览验证

- 页面：地址确认页
- 目标SPM：takeaway_pickup.address_confirm
- 测试类型：pv_check
- 预期结果：页面浏览上报成功，页面SPM与page_id符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 页面进入 | 进入地址确认页 | 页面浏览上报 | 确认页面浏览是否正常上报，page_id是否正确。 |
## 地址表单区区块曝光验证

- 页面：地址确认页
- 目标SPM：takeaway_pickup.address_confirm.address_form
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入地址确认页并确认地址表单区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 地址卡片参数验证

- 页面：地址确认页
- 目标SPM：takeaway_pickup.address_confirm.address_form.address_card
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发地址卡片对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发地址卡片对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发地址卡片对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发地址卡片对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发地址卡片对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发地址卡片对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发地址卡片对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 改收货人按钮坑位点击验证

- 页面：地址确认页
- 目标SPM：takeaway_pickup.address_confirm.address_actions.edit_receiver_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击改收货人按钮 | 点击事件上报 | 无biz_type要求。 |
## 改收货人按钮参数验证

- 页面：地址确认页
- 目标SPM：takeaway_pickup.address_confirm.address_actions.edit_receiver_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发改收货人按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发改收货人按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发改收货人按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发改收货人按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发改收货人按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发改收货人按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发改收货人按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 改手机号按钮坑位点击验证

- 页面：地址确认页
- 目标SPM：takeaway_pickup.address_confirm.address_actions.edit_phone_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击改手机号按钮 | 点击事件上报 | 无biz_type要求。 |
## 改手机号按钮参数验证

- 页面：地址确认页
- 目标SPM：takeaway_pickup.address_confirm.address_actions.edit_phone_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发改手机号按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发改手机号按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发改手机号按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发改手机号按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发改手机号按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发改手机号按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发改手机号按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 改地址按钮坑位点击验证

- 页面：地址确认页
- 目标SPM：takeaway_pickup.address_confirm.address_actions.edit_address_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击改地址按钮 | 点击事件上报 | 无biz_type要求。 |
## 改地址按钮参数验证

- 页面：地址确认页
- 目标SPM：takeaway_pickup.address_confirm.address_actions.edit_address_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发改地址按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发改地址按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发改地址按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发改地址按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发改地址按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发改地址按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发改地址按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 确认地址按钮坑位点击验证

- 页面：地址确认页
- 目标SPM：takeaway_pickup.address_confirm.fixed_bottom.confirm_address_btn
- 测试类型：position_click_check
- 预期结果：坑位点击上报成功，SPM与业务类型符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 点击坑位 | 点击确认地址按钮 | 点击事件上报 | 无biz_type要求。 |
## 确认地址按钮参数验证

- 页面：地址确认页
- 目标SPM：takeaway_pickup.address_confirm.fixed_bottom.confirm_address_btn
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发确认地址按钮对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发确认地址按钮对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发确认地址按钮对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发确认地址按钮对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发确认地址按钮对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发确认地址按钮对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发确认地址按钮对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 地址修改页页面浏览验证

- 页面：地址修改页
- 目标SPM：takeaway_pickup.address_edit
- 测试类型：pv_check
- 预期结果：页面浏览上报成功，页面SPM与page_id符合方案定义。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 页面进入 | 进入地址修改页 | 页面浏览上报 | 确认页面浏览是否正常上报，page_id是否正确。 |
## 地址表单区区块曝光验证

- 页面：地址修改页
- 目标SPM：takeaway_pickup.address_edit.address_form
- 测试类型：block_exposure_check
- 预期结果：区块曝光上报成功，目标区块标识正确。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 区块首次曝光 | 进入地址修改页并确认地址表单区展示 | 区块曝光上报 | 确认区块曝光是否按方案触发。 |
## 地址卡片参数验证

- 页面：地址修改页
- 目标SPM：takeaway_pickup.address_edit.address_form.address_card
- 测试类型：param_check
- 预期结果：关键业务参数完整上报，字段定义符合方案约束。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 用户PIN参数校验 | 触发地址卡片对应事件 | user_pin | 用户PIN需符合类型 string，且为必填。 |
| 订单ID参数校验 | 触发地址卡片对应事件 | order_id | 订单ID需符合类型 string，且为必填。 |
| 车辆VID参数校验 | 触发地址卡片对应事件 | vid | 车辆VID需符合类型 string，且为必填。 |
| 运营城市参数校验 | 触发地址卡片对应事件 | car_city | 运营城市需符合类型 string，且为必填。 |
| 环境参数校验 | 触发地址卡片对应事件 | env | 环境需符合类型 string，且为必填。 |
| 触发方式参数校验 | 触发地址卡片对应事件 | click_type | 触发方式需符合类型 string，且为必填。 |
| 推荐Sug词参数校验 | 触发地址卡片对应事件 | reco_sug | 推荐Sug词需符合类型 array_string。 |
## 核心业务链路链路验证

- 页面：首页
- 目标SPM：takeaway_pickup_home -> takeaway_pickup_order_detail
- 测试类型：flow_check
- 预期结果：核心页面跳转链路可被完整识别，页面序列与方案一致。

| 检查点 | 操作 | 检查目标 | 备注 |
| --- | --- | --- | --- |
| 链路步骤1 | 进入首页 | takeaway_pickup_home | 首页展示外卖自提推荐词入口。 |
| 链路步骤2 | 从首页推荐词进入商品列表页 | takeaway_pickup_product_list | 导购阶段的门店和商品列表页。 |
| 链路步骤3 | 进入商品列表页 | takeaway_pickup_product_list | 导购阶段的门店和商品列表页。 |
| 链路步骤4 | 选择商品进入商品确认页 | takeaway_pickup_item_confirm | 单品确认和快捷改配页面。 |
| 链路步骤5 | 进入结算页并点击去支付按钮 | takeaway_pickup_checkout | 订单结算和支付确认页面。 |
| 链路步骤6 | 进入支付中页 | takeaway_pickup_paying | 支付处理中状态页。 |
| 链路步骤7 | 进入支付成功页 | takeaway_pickup_pay_success | 支付成功结果页。 |
| 链路步骤8 | 进入订单详情页 | takeaway_pickup_order_detail | 订单详情和取餐码页面。 |
